{
  "name": "anorak-arcade-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Anorak Arcade — native iOS shell (Capacitor) wrapping the public/ web arcade.",
  "scripts": {
    "sync": "cap sync ios",
    "open": "cap open ios",
    "ios": "cap sync ios && cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0",
    "@capacitor/filesystem": "^7.0.0",
    "@capacitor/haptics": "^7.0.0",
    "@capacitor/keyboard": "^7.0.0",
    "@capacitor-community/apple-sign-in": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
